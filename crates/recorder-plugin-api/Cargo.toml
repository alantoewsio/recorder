[package]
name = "recorder-plugin-api"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Stable C ABI (v1) for out-of-process / dylib audio plugins"

[dependencies]
